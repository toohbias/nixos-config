{

  xdg = {
    enable = true;

    userDirs = {
      enable = true;

      desktop = null;
      download = "$HOME/dld";
      documents = "$HOME/doc";
      templates = null;
      music = null;
      videos = "$HOME/vid";
      pictures = "$HOME/pic";
      publicShare = null;
    };
  };

}
